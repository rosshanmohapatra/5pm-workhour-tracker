.DS_Store
Thumbs.db
*.log

.vercel

# Environment secrets — NEVER commit these
.env
.env.local
.env.*.local

# Supabase client config — contains the anon key.
# To stop tracking: set VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY in Vercel env vars,
# generate lib/config.js at build time, then uncomment the line below and run:
#   git rm --cached lib/config.js
lib/config.js

# node_modules
node_modules/
