.DS_Store
Thumbs.db
*.log

.vercel

# Environment secrets — NEVER commit these
.env
.env.local
.env.*.local

# Supabase client config — anon key is intentionally public (enforced by RLS).
# Kept in git so Vercel deploys without a build step.
# lib/config.js

# node_modules
node_modules/

.vercel

# Local-only files: OAuth client id note, generated reports, scratch assets
Github_5PM_Client_ID.txt
*-report-*.pdf
*-report-*.csv
test-inline.pdf
