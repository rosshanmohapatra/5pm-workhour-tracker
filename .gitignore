.DS_Store
Thumbs.db
*.log

.vercel

# Environment secrets — NEVER commit these
.env
.env.local
.env.*.local

# Supabase client config — anon key is intentionally public (enforced by RLS).
# Kept in git so Vercel deploys without a build step.
# lib/config.js

# node_modules
node_modules/
